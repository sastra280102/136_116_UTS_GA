<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a116136utsga">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.116136UTSGA">

        <activity
            android:name=".LaporLokasi"
            android:exported="false"
            android:label="Laporan Lokasi" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDu5b5KWzBCvdplSNUGkham_tMPale6l2M" />

        <activity
            android:name=".CariLokasi"
            android:exported="false"
            android:label="Cari Lokasi" />



        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Lokasi Saat Ini">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>